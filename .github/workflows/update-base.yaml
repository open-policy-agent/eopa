name: Update base image

on:
  workflow_dispatch: {}
  push:
    branches: [main]

jobs:
  prereqs:
    name: Prereqs
    runs-on: ubuntu-24.04
    steps:
      - uses: StyraInc/styra-init-action@main

  deploy:
    name: Build and push base images
    needs: prereqs
    runs-on: ubuntu-24.04
    steps:
      - name: Tune GitHub-hosted runner network
        uses: smorimoto/tune-github-hosted-runner-network@bb252dcb5c8609a31087e7993daa086f5a1c0069 # v1.0.0
      - name: Check out code
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
        with:
          fetch-depth: 0
      # - name: Build & Publish OCI
      #   id: apko
      #   uses: chainguard-images/actions/apko-publish@711a62d32118ee565db9c24249fc90011bdd7eb4
      #   with:
      #     config: apko.yaml
      #     archs: x86_64,aarch64
      #     tag: ghcr.io/styrainc/eopa-base:latest # TODO
      # - name: Build & Publish OCI (Debug)
      #   id: apko-debug
      #   uses: chainguard-images/actions/apko-publish@711a62d32118ee565db9c24249fc90011bdd7eb4
      #   with:
      #     config: apko-debug.yaml
      #     archs: x86_64,aarch64
      #     tag: ghcr.io/styrainc/eopa-debug:latest # TODO
