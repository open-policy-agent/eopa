! exec eopa lint
! stdout .
stderr 'Error: at least one file or directory must be provided for linting'
stderr 'Usage:'
stderr 'eopa lint <path> \[path \[...\]\] \[flags\]'

exec eopa lint --help
! stderr .
stdout 'Lint Rego source files for linter rule violations.'
stdout 'Usage:'
stdout 'eopa lint <path> \[path \[...\]\] \[flags\]'
stdout 'Flags:'
