# This test ensures that `eopa test` correctly accepts extra data to insert
# into the `data.` hierarchy from its arguments. This functionality is part of
# baseline OPA, but this is a good sanity check.
exec eopa test ./test1.rego ./dummy1.rego 
! stderr .
cmp stdout exp/stdout
-- test1.rego --
package testpkg
test_data_access if {
	"abc" == data.foo.bar
}
-- dummy1.rego --
package foo
bar := "abc"
-- exp/stdout --
PASS: 1/1
