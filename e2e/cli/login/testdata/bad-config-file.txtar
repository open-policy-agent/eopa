! exec eopa login
stderr 'Error: config file'
# testscript freaks out when it sees a windows-style path here
[!windows] stderr $WORK/.styra.yaml
stderr 'While parsing config: yaml: mapping values are not allowed in this context'
! stderr 'Usage:'
! stdout .

-- .styra.yaml --
u: u: u:
