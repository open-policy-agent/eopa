# file already exists, updates perms
chmod 777 .styra-session
ttyin -stdin pasted-token
exec eopa login --read-token
stderr 'Successfully stored token'
cmp .styra-session pasted-token
exec ls -l .styra-session
stdout '-rwx------'

-- pasted-token --
uninteresting

-- .styra-session --
old token
