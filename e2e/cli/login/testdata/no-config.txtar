# no config whatsoever
! exec eopa login
stderr 'Error: URL not provided: use .styra.yaml or pass `--url`' 
! stderr 'Usage:'
! stdout .

# not a url
! exec eopa login --url https://foo:12:baz
stderr 'Error: invalid URL https://foo:12:baz: parse "https://foo:12:baz": invalid port ":baz" after host'
! stderr 'Usage:'
! stdout .

# bad scheme
! exec eopa login --url file://das
stderr 'Error: invalid URL file://das: scheme must be http\[s\]'
! stderr 'Usage:'
! stdout .
